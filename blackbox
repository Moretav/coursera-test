// how ican fixed this codevar var speakWord = "Good Bye";function speak(name) { console.log(speakWord + " " + name)}?
  
  
var speakWord = "Good Bye";


function speak(name) {
  console.log(speakWord + " " + name);
}
